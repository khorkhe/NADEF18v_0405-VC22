# این requirements مخصوص ساخت exe روی Windows 7 (32-bit) با Python 3.8 است.
# نکته مهم: پایتون 3.8 آخرین نسخه‌ای است که رسمی از Windows 7 پشتیبانی می‌کند
# (از پایتون 3.9 به بعد، ویندوز 7 دیگر پشتیبانی نمی‌شود).
# باید نسخه‌ی "Windows x86" (یعنی 32-bit) پایتون 3.8.10 را از python.org نصب کنی،
# نه نسخه‌ی 64-bit (amd64).

pillow==10.4.0
# Pillow از نسخه‌ی 11.0.0 به بعد دیگر پایتون 3.8 را پشتیبانی نمی‌کند؛
# 10.4.0 آخرین نسخه‌ی سازگار با پایتون 3.8 است.

pywin32==306
# نسخه‌ای پایدار و شناخته‌شده که با پایتون 3.8 (32-bit) به‌خوبی کار می‌کند.

python-dateutil
persiantools
pyinstaller
